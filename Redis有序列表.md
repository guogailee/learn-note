## Zset

```
Zset：
结构类似set，每个元素有一个分值score，所有元素按照分值的大小进行排列，相当于有序链表。

底层实现：
跳跃表
```

## 跳跃表

```
L2:部分L1元素

L1：部分L0元素

L0:所有的元素，根据score排序

所有的元素都会在L0层的链表中，根据分数进行排序，同时会有一部分节点有机会被抽取到L1层中，作为一个稀疏索引，L1层也有一部分会被抽取到L2层，组成更稀疏的索引列表。

查找的时候，先从L2定位->L1->L0
```

